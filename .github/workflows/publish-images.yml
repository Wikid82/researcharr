name: Publish images (disabled; use release workflow)

on:
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; image publishing moved to release workflow."
