# Feature branch compose files (ignore user-specific or experimental compose variants)
docker-compose.feat.yml
.venv/
.venv
.env
.env.*
.env.example
.env.example.local
!.env.example
# Local test helper script (if present) — ignore generated wrappers
script.sh

# Bytecode, caches and editor files
__pycache__/
researcharr/__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.tox/
.cache/
.ropeproject/
.venv/
venv/
env/

# Editor/IDE
.idea/

# Coverage and test reports
# Allow `coverage.xml` to be tracked (we generate it during CI and publish
# it as an artifact). Keep per-run binary data and HTML report directories
# ignored.
*.coverage
htmlcov/

# Build and distribution
dist/
build/
build/artifacts/
build/smoke-logs*
*.tar.gz
*.egg-info/
pip-wheel-metadata/

# Local container build directory (ignore user-specific image build artifacts)
container_build/

# Local DBs, logs and runtime artifacts (environment-specific)
*.sqlite3
*.sqlite
*.db
*.log
.log

# Persisted runtime files we create under CONFIG_DIR (ignore generated runtime data)
/config/general.yml
/config/updates_cache.yml
/config/updates/
/config/updates/downloads/
/config/backups/
/config/task_history.jsonl
## webui user file removed; DB-backed users in sqlite
# config/webui_user.yml
# config/webui_user.yml.bak*
config/*.db
config/*.sqlite
config/cron.log
config/logs/
config/*.log

# Trivy scan outputs and cache
trivy-*.json
**/trivy-*.json
.trivy/
.trivy/**

# local sqlite DB
researcharr.db
*.db-journal
researcharr.db-journal
# Ignore mirror clones and any large mirrored repo directories accidentally created
repo-mirror.git/
repo-*.git/

# Coverage report generated by CI
coverage.json
coverage.xml

## Local virtualenvs created by me or CI runs
.venv*

# Bandit reports and other security scanner outputs
bandit-report*.json

# Local Bandit debug/verbose outputs
bandit-debug-*.log
static/banner.xcf

# File Types
*.xcf

# Additional tooling caches & local artifacts
.ruff_cache/
node_modules/
.tmp_coverage/
logs/
