isort==7.0.0
# Pin ruff to an available release compatible with Python 3.10
ruff==0.14.4
bandit==1.8.1
pre-commit
pytest==9.0.0
pytest-cov
types-requests
types-PyYAML
types-Markdown

# Optional packages used by pipeline features (convenience for local dev)
PyYAML==6.0.3
jsonschema==4.25.1; python_version < "3.14"
prometheus-client==0.23.1

# Optional tooling (development-only)
setuptools_scm
python-json-logger
debugpy
# Unpinned ruff invocation below removed (already pinned above)
ipython
ipdb
tox
pip-tools
bandit
safety==3.7.0
pytest-xdist
detect-secrets

# Documentation tooling
mkdocs
mkdocs-material
pip-tools
