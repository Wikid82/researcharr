# Example .env file for local docker-compose overrides.
# Copy this to `.env` and adjust host paths to your machine. `.env` is
# intentionally ignored by the repository (see `.gitignore`) so your local
# absolute paths aren't committed.

# Host path mounted into the container as /app (development only).
# Default: project root
HOST_APP_PATH=./

# Host path mounted into the container as /config
# Default: ./config
HOST_CONFIG_PATH=./config

# Host path for logs mapped to /config/logs
# Default: ./logs
HOST_LOGS_PATH=./logs
