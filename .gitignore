.venv
.env
script.sh
__pycache__/
*.pyc
.pytest_cache/
researcharr/__pycache__/
.pytest_cache/
*.pyo
*.pyd
*.log
*.egg-info/
*.coverage
.coverage*
htmlcov/
dist/
build/
# CI and local build artifacts
build/
build/artifacts/
build/smoke-logs*
*.tar.gz
*.sqlite3
keeping

# Persisted runtime files we create under CONFIG_DIR
/config/general.yml
/config/updates_cache.yml
/config/updates/
/config/updates/downloads/
/config/backups/
/config/task_history.jsonl

# Generated webui user credentials (do not commit)
config/webui_user.yml
config/webui_user.yml.bak*

# Runtime DBs and logs under config/ (environment-specific)
config/*.db
config/*.sqlite
config/cron.log
config/logs/
config/*.log

# Trivy scan outputs
trivy-*.json
**/trivy-*.json

# Trivy local cache/artifact folder (if created by CI locally)
.trivy/
.trivy/**

# Editor and IDE settings
.vscode/

# Local DBs at repo root
*.db
*.sqlite3

# Local scratch / duplicate folder sometimes created during edits.
# This ignores the local scratch area (if present) to avoid accidental
# commits of duplicate files named like `researcharr.py`.
# Use `backups-scratch/` for local, ignored backup/editing helpers.
/backups-scratch/
