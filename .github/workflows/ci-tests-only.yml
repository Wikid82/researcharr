name: CI — Tests only (disabled; consolidated into main CI)

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled; use ci.yml instead."
