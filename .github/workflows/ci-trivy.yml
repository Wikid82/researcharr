name: CI — Trivy scan (disabled during development)

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled during development; re-enable for release workflow when needed."
