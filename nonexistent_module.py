# Dummy module to satisfy tests that import a missing module for error handling
# Intentionally empty — tests expecting ImportError will still pass because
# they catch ImportError; providing this file only silences editor import warnings.
