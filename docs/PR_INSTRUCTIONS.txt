Suggested git commands to create a branch, add the new docs, and open a PR:

# Create a branch
git checkout -b docs/add-local-docs-and-contributing

# Add files
git add docs/ CONtributing.md .github/PULL_REQUEST_TEMPLATE.md

# Commit
git commit -m "docs: add local docs/ copies of wiki pages and CONTRIBUTING + PR template"

# Push
git push -u origin docs/add-local-docs-and-contributing

# Open a PR on GitHub targeting the `development` branch. Title suggestion:
# "docs: add in-repo docs + CONTRIBUTING guide and PR template"

# Suggested PR body:
# - Adds `researcharr/docs/` containing contributor-facing docs copied from the wiki (Contributing, CI-and-Development, Packaging-and-Distribution, Recent-Changes, Changelog, Health-and-Metrics, Roadmap, Getting-Started).
# - Adds a repo-level `CONTRIBUTING.md` for quick developer setup.
# - Adds a GitHub PR template reminding contributors to run isort/black/pre-commit/tests and target `development`.
# Rationale: make docs available directly in the repository so contributors and CI bots don't need to open the wiki to access critical guidance.
