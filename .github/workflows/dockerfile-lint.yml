name: Dockerfile lint (disabled; consolidated into main CI)

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; Dockerfile linting now runs in main CI workflow (ci.yml) via pre-commit hooks."
