name: CI — Build (disabled; consolidated into main CI)

on:
  workflow_dispatch: {}

# By default skip Docker build/publish tasks to keep CI focused on fast
# tests and linting. Change to 'false' when you want to re-enable image builds.
env:
  SKIP_DOCKER: 'true'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled; use ci.yml instead."
